FROM mcr.microsoft.com/devcontainers/base:ubuntu

# Placeholder for custom tooling. Example:
# RUN apt-get update && apt-get install -y --no-install-recommends \
#     vim less jq && rm -rf /var/lib/apt/lists/*


